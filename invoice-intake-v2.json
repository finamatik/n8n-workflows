{
  "name": "Check supplier invoices from Gmail against purchase orders and post them to Google Sheets",
  "nodes": [
    {
      "parameters": {
        "simple": false,
        "filters": {
          "labelIds": [
            "<__PLACEHOLDER_VALUE__Gmail label your supplier invoices land in, e.g. \"Invoices\"__>"
          ],
          "hasAttachment": true
        },
        "options": {
          "downloadAttachments": true
        },
        "pollTimes": {
          "item": [
            {
              "mode": "everyMinute"
            }
          ]
        }
      },
      "type": "n8n-nodes-base.gmailTrigger",
      "typeVersion": 1.2,
      "position": [
        -240,
        220
      ],
      "id": "invoice-arrives-by-email",
      "name": "Invoice arrives by email"
    },
    {
      "parameters": {
        "options": {},
        "conditions": {
          "options": {
            "version": 2,
            "leftValue": "",
            "caseSensitive": false,
            "typeValidation": "loose"
          },
          "combinator": "and",
          "conditions": [
            {
              "id": "pdf1",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              },
              "leftValue": "={{ Object.values($binary || {}).some(function(b){ return String(b && b.fileExtension || '').toLowerCase() === 'pdf' || String(b && b.mimeType || '') === 'application/pdf'; }) }}",
              "rightValue": ""
            }
          ]
        },
        "looseTypeValidation": true
      },
      "type": "n8n-nodes-base.filter",
      "typeVersion": 2.2,
      "position": [
        -20,
        220
      ],
      "id": "keep-only-pdf-attachments",
      "name": "Keep only PDF attachments"
    },
    {
      "parameters": {
        "options": {},
        "operation": "pdf",
        "binaryPropertyName": "={{ Object.keys($binary || {}).find(function(k){ return String($binary[k].fileExtension || '').toLowerCase() === 'pdf' || String($binary[k].mimeType || '') === 'application/pdf'; }) }}"
      },
      "type": "n8n-nodes-base.extractFromFile",
      "typeVersion": 1,
      "position": [
        200,
        220
      ],
      "id": "read-the-pdf",
      "name": "Read the PDF"
    },
    {
      "parameters": {
        "options": {
          "temperature": 0
        }
      },
      "type": "@n8n/n8n-nodes-langchain.lmChatOpenAi",
      "typeVersion": 1.3,
      "position": [
        420,
        420
      ],
      "id": "extraction-model",
      "name": "Extraction model"
    },
    {
      "parameters": {
        "text": "={{ $json.text }}",
        "options": {
          "systemPromptTemplate": "You read supplier invoices and return structured data. Accuracy matters more than completeness.\n\n1. Return null for any field you cannot read with confidence. Never guess a number.\n2. Amounts are numbers. No currency symbols, no thousands separators.\n3. Dates are ISO yyyy-mm-dd. If the format is ambiguous, prefer day first, which is the Gulf convention.\n4. Take the FINAL total, which on a multi page invoice is on the last page. Never take a subtotal from page one.\n5. A credit note carries negative amounts. If the document says credit note or refund, return every amount as a negative number.\n6. The supplier is who issued the invoice, not who received it. If a logo and a bill to address disagree, the supplier is the one with the tax registration number.\n7. Tax registration numbers are digits only. Strip spaces and labels such as TRN or VAT No."
        },
        "schemaType": "fromJson",
        "jsonSchemaExample": "{\n  \"supplier\": \"Gulf Office Supplies FZCO\",\n  \"supplier_tax_number\": \"100123456700003\",\n  \"invoice_number\": \"INV-2041\",\n  \"po_number\": \"PO-8814\",\n  \"issue_date\": \"2026-08-14\",\n  \"due_date\": \"2026-09-13\",\n  \"currency\": \"AED\",\n  \"subtotal\": 4200.0,\n  \"tax_amount\": 210.0,\n  \"total\": 4410.0,\n  \"line_items\": [\n    {\n      \"description\": \"A4 paper, 5 boxes\",\n      \"quantity\": 5,\n      \"unit_price\": 840.0,\n      \"amount\": 4200.0\n    }\n  ]\n}"
      },
      "type": "@n8n/n8n-nodes-langchain.informationExtractor",
      "typeVersion": 1.2,
      "position": [
        420,
        220
      ],
      "id": "pull-the-invoice-fields",
      "name": "Pull the invoice fields"
    },
    {
      "parameters": {
        "options": {},
        "sheetName": {
          "__rl": true,
          "mode": "name",
          "value": "SupplierMaster"
        },
        "documentId": {
          "__rl": true,
          "mode": "url",
          "value": "<__PLACEHOLDER_VALUE__URL of the spreadsheet holding SupplierMaster, OpenPurchaseOrders, Ledger and Exceptions__>"
        }
      },
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.5,
      "position": [
        660,
        220
      ],
      "id": "supplier-master",
      "name": "Supplier master"
    },
    {
      "parameters": {
        "options": {},
        "sheetName": {
          "__rl": true,
          "mode": "name",
          "value": "OpenPurchaseOrders"
        },
        "documentId": {
          "__rl": true,
          "mode": "url",
          "value": "<__PLACEHOLDER_VALUE__URL of the same spreadsheet__>"
        }
      },
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.5,
      "position": [
        880,
        220
      ],
      "id": "open-purchase-orders",
      "name": "Open purchase orders"
    },
    {
      "parameters": {
        "options": {},
        "sheetName": {
          "__rl": true,
          "mode": "name",
          "value": "Ledger"
        },
        "documentId": {
          "__rl": true,
          "mode": "url",
          "value": "<__PLACEHOLDER_VALUE__URL of the same spreadsheet__>"
        }
      },
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.5,
      "position": [
        1100,
        220
      ],
      "id": "invoices-already-posted",
      "name": "Invoices already posted"
    },
    {
      "parameters": {
        "jsCode": "// Every rule an accounts payable clerk applies by hand, in one place.\n// Nothing here talks to an API. It compares what the model read off the PDF\n// against the supplier master, the open purchase orders and what is already\n// in the ledger, then says post it, hold it, or reject it, and why.\n\nconst inv = $('Pull the invoice fields').first().json.output || $('Pull the invoice fields').first().json;\n\nconst suppliers = $('Supplier master').all().map(i => i.json);\nconst orders    = $('Open purchase orders').all().map(i => i.json);\nconst posted    = $('Invoices already posted').all().map(i => i.json);\n\n// ---- tolerances you may want to change -------------------------------------\nconst ROUNDING_TOLERANCE = 0.02;   // currency units allowed on subtotal + tax = total\nconst PO_TOLERANCE_PCT   = 2;      // percent an invoice may exceed the PO line\nconst MAX_AGE_DAYS       = 120;    // older than this is almost always a duplicate claim\nconst APPROVAL_LIMIT     = 10000;  // above this a human signs it off even when clean\nconst VALID_TAX_RATES    = [0, 5]; // UAE standard and zero rated\n// ----------------------------------------------------------------------------\n\nconst norm = s => String(s == null ? '' : s).toLowerCase().replace(/\\b(llc|l\\.l\\.c|fzco|fze|fzc|ltd|limited|inc|co|company|est|establishment|trading llc)\\b/g, '').replace(/[^a-z0-9]/g, '').trim();\nconst num  = v => { const s = String(v == null ? '' : v).replace(/[^0-9.\\-]/g, ''); if (s === '' || s === '-' || s === '.') return null; const n = Number(s); return Number.isFinite(n) ? n : null; };\nconst days = d => { const t = Date.parse(d); return Number.isFinite(t) ? Math.round((Date.now() - t) / 86400000) : null; };\n\nconst failures = [];\nconst warnings = [];\n\n// 1. Did the model actually read the document\nconst required = ['supplier', 'invoice_number', 'issue_date', 'total'];\nconst missing = required.filter(f => inv[f] === null || inv[f] === undefined || inv[f] === '');\nif (missing.length) failures.push('Could not read ' + missing.join(', ') + ' from the document');\n// A total of \"lots\" is not a total. Blank is obvious; unreadable is the one\n// that slips through, because the field is populated and looks fine.\nif (!missing.includes('total') && num(inv.total) === null) {\n  failures.push('The total reads \"' + inv.total + '\", which is not an amount');\n}\n\n// 2. Is this supplier one we buy from\nconst supplier = suppliers.find(s => norm(s.supplier_name) === norm(inv.supplier));\nif (!supplier) failures.push('Supplier \"' + inv.supplier + '\" is not on the supplier master');\n// On the master is not the same as still trading with us. A column called\n// status, state or active carries that; anything but active stops the invoice.\nelse {\n  const state = String(supplier.status || supplier.state || supplier.active || 'active').trim().toLowerCase();\n  if (['archived', 'inactive', 'blocked', 'on hold', 'hold', 'closed', 'no', 'false'].indexOf(state) !== -1) {\n    failures.push('Supplier \"' + supplier.supplier_name + '\" is marked ' + state + ' on the supplier master');\n  }\n}\n\n// 2b. The tax number on the invoice has to be the one we hold for them.\n// This is the check that catches an invoice that looks right and is not.\nif (supplier) {\n  const onInvoice = String(inv.supplier_tax_number || '').replace(/\\D/g, '');\n  const onMaster  = String(supplier.supplier_tax_number || supplier.tax_number || '').replace(/\\D/g, '');\n  if (onInvoice && onMaster && onInvoice !== onMaster) {\n    failures.push('The tax registration number on the invoice does not match the one we hold for this supplier');\n  }\n}\n\n// 3. Have we already paid this one\nconst dupe = posted.find(p => norm(p.supplier) === norm(inv.supplier) && String(p.invoice_number).trim() === String(inv.invoice_number).trim());\nif (dupe) failures.push('Invoice ' + inv.invoice_number + ' from this supplier is already posted on row ' + (dupe.row_number || 'unknown'));\n\n// 4. Does the invoice add up\nconst sub = num(inv.subtotal), tax = num(inv.tax_amount), tot = num(inv.total);\nif (sub !== null && tax !== null && tot !== null) {\n  const diff = Math.abs((sub + tax) - tot);\n  if (diff > ROUNDING_TOLERANCE) failures.push('Subtotal ' + sub + ' plus tax ' + tax + ' is ' + (sub + tax).toFixed(2) + ', but the invoice total says ' + tot);\n}\n\n// 5. Is the tax rate one that exists here\nif (sub !== null && tax !== null && sub > 0) {\n  const rate = Math.round((tax / sub) * 1000) / 10;\n  if (!VALID_TAX_RATES.some(r => Math.abs(rate - r) < 0.3)) failures.push('Tax works out at ' + rate + ' percent, which is not a rate in use');\n}\n\n// 6. A tax number when tax is charged\nif (tax !== null && tax > 0) {\n  const trn = String(inv.supplier_tax_number || '').replace(/\\D/g, '');\n  if (!trn) failures.push('Tax is charged but the invoice shows no supplier tax registration number');\n  else if (trn.length !== 15) failures.push('Supplier tax registration number is ' + trn.length + ' digits, it should be 15');\n}\n\n// 7. Does it match an open purchase order\nlet po = null;\nif (inv.po_number) {\n  po = orders.find(o => String(o.po_number).trim().toLowerCase() === String(inv.po_number).trim().toLowerCase());\n  if (!po) failures.push('Purchase order ' + inv.po_number + ' is not open');\n  else {\n    const remaining = num(po.remaining_value);\n    if (remaining !== null && tot !== null && tot > remaining * (1 + PO_TOLERANCE_PCT / 100)) {\n      failures.push('Invoice of ' + tot + ' exceeds the ' + remaining + ' left on purchase order ' + po.po_number);\n    }\n    if (po.currency && inv.currency && String(po.currency).toUpperCase() !== String(inv.currency).toUpperCase()) {\n      failures.push('Invoice is in ' + inv.currency + ' but purchase order ' + po.po_number + ' is in ' + po.currency);\n    }\n  }\n} else if (supplier && String(supplier.po_required).toLowerCase() === 'yes') {\n  failures.push('This supplier requires a purchase order and none is quoted on the invoice');\n}\n\n// 8. Is the date sane\nconst age = days(inv.issue_date);\nif (age === null) failures.push('Issue date \"' + inv.issue_date + '\" is not a date we can read');\nelse if (age < -1) failures.push('Issue date is in the future');\nelse if (age > MAX_AGE_DAYS) failures.push('Invoice is dated ' + age + ' days ago, past the '\n  + MAX_AGE_DAYS + ' day limit, which is almost always a duplicate claim');\n\n// ---- the decision ----------------------------------------------------------\nlet decision, reason;\nif (failures.length) {\n  decision = 'reject';\n  reason = failures.join('. ');\n} else if (tot !== null && tot < 0) {\n  // A credit note has to be set against the right original bill, and only a\n  // person knows which one. Posting it automatically is a wrong entry, not a\n  // saved minute.\n  decision = 'approve';\n  reason = 'This is a credit note, so a person posts it against the right bill';\n} else if (tot !== null && tot > APPROVAL_LIMIT) {\n  decision = 'approve';\n  reason = 'Every check passed but ' + tot + ' ' + (inv.currency || '') + ' is above the ' + APPROVAL_LIMIT + ' limit, so it needs a signature';\n} else {\n  decision = 'post';\n  reason = 'Every check passed and the amount is inside the limit';\n}\n\nreturn [{\n  json: {\n    decision: decision,\n    reason: reason,\n    failed_checks: failures.length,\n    // Strings, not arrays. An array put into a spreadsheet cell or an email\n    // body becomes whatever JavaScript decides to make of it; a numbered\n    // string reads the same everywhere it lands.\n    failures: failures.map(function (f, i) { return (i + 1) + '. ' + f; }).join('\\n'),\n    warnings: warnings.join('. '),\n    supplier: inv.supplier,\n    supplier_email: supplier ? supplier.supplier_email : null,\n    approver_email: po ? po.approver_email : (supplier ? supplier.default_approver : null),\n    invoice_number: inv.invoice_number,\n    po_number: inv.po_number || null,\n    issue_date: inv.issue_date,\n    due_date: inv.due_date || null,\n    currency: inv.currency || null,\n    subtotal: sub,\n    tax_amount: tax,\n    total: tot,\n    checked_at: new Date().toISOString()\n  }\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1320,
        220
      ],
      "id": "run-the-checks",
      "name": "Run the checks"
    },
    {
      "parameters": {
        "options": {},
        "conditions": {
          "options": {
            "version": 2,
            "leftValue": "",
            "caseSensitive": true,
            "typeValidation": "loose"
          },
          "combinator": "and",
          "conditions": [
            {
              "id": "d1",
              "operator": {
                "type": "string",
                "operation": "notEquals"
              },
              "leftValue": "={{ $json.decision }}",
              "rightValue": "reject"
            }
          ]
        },
        "looseTypeValidation": true
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        1540,
        220
      ],
      "id": "did-every-check-pass?",
      "name": "Did every check pass?"
    },
    {
      "parameters": {
        "options": {},
        "conditions": {
          "options": {
            "version": 2,
            "leftValue": "",
            "caseSensitive": true,
            "typeValidation": "loose"
          },
          "combinator": "and",
          "conditions": [
            {
              "id": "d2",
              "operator": {
                "type": "string",
                "operation": "equals"
              },
              "leftValue": "={{ $json.decision }}",
              "rightValue": "post"
            }
          ]
        },
        "looseTypeValidation": true
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        1760,
        120
      ],
      "id": "inside-the-approval-limit?",
      "name": "Inside the approval limit?"
    },
    {
      "parameters": {
        "columns": {
          "value": {},
          "mappingMode": "autoMapInputData",
          "matchingColumns": []
        },
        "options": {},
        "operation": "append",
        "sheetName": {
          "__rl": true,
          "mode": "name",
          "value": "Ledger"
        },
        "documentId": {
          "__rl": true,
          "mode": "url",
          "value": "<__PLACEHOLDER_VALUE__URL of the same spreadsheet__>"
        }
      },
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.5,
      "position": [
        1990,
        20
      ],
      "id": "post-it-to-the-ledger",
      "name": "Post it to the ledger"
    },
    {
      "parameters": {
        "text": "=Invoice {{ $json.invoice_number }} from {{ $json.supplier }} for {{ $json.total }} {{ $json.currency }} passed every check but is above the approval limit.\nPurchase order: {{ $json.po_number || 'none quoted' }}\nApprover: {{ $json.approver_email || 'not set on the supplier master' }}\nReply in thread to approve.",
        "select": "channel",
        "channelId": {
          "__rl": true,
          "mode": "name",
          "value": "<__PLACEHOLDER_VALUE__Slack channel for accounts payable, e.g. #finance-ap__>"
        },
        "otherOptions": {}
      },
      "type": "n8n-nodes-base.slack",
      "typeVersion": 2.2,
      "position": [
        1990,
        220
      ],
      "id": "ask-a-human-to-sign-it-off",
      "name": "Ask a human to sign it off"
    },
    {
      "parameters": {
        "columns": {
          "value": {},
          "mappingMode": "autoMapInputData",
          "matchingColumns": []
        },
        "options": {},
        "operation": "append",
        "sheetName": {
          "__rl": true,
          "mode": "name",
          "value": "Exceptions"
        },
        "documentId": {
          "__rl": true,
          "mode": "url",
          "value": "<__PLACEHOLDER_VALUE__URL of the same spreadsheet__>"
        }
      },
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.5,
      "position": [
        1760,
        420
      ],
      "id": "park-it-in-the-exceptions-queue",
      "name": "Park it in the exceptions queue"
    },
    {
      "parameters": {
        "sendTo": "={{ $json.supplier_email }}",
        "subject": "=We cannot process invoice {{ $json.invoice_number }} yet",
        "message": "={{ 'Hello,\\n\\nWe received invoice ' + $json.invoice_number + ' but cannot put it through accounts payable yet.\\n\\n' + $json.failures + '\\n\\nPlease send a corrected invoice and we will process it on the same terms.\\n\\nAccounts Payable' }}",
        "options": {}
      },
      "type": "n8n-nodes-base.gmail",
      "typeVersion": 2.2,
      "position": [
        1990,
        420
      ],
      "id": "tell-the-supplier-what-is-wrong",
      "name": "Tell the supplier what is wrong"
    },
    {
      "parameters": {},
      "type": "n8n-nodes-base.errorTrigger",
      "typeVersion": 1,
      "position": [
        1320,
        640
      ],
      "id": "if-the-run-breaks",
      "name": "If the run breaks"
    },
    {
      "parameters": {
        "text": "=Invoice intake failed on \"{{ $json.execution.lastNodeExecuted }}\".\n{{ $json.execution.error.message }}\nNo invoice was posted. Open the execution and rerun it once the cause is fixed.",
        "select": "channel",
        "channelId": {
          "__rl": true,
          "mode": "name",
          "value": "<__PLACEHOLDER_VALUE__Slack channel for accounts payable, e.g. #finance-ap__>"
        },
        "otherOptions": {}
      },
      "type": "n8n-nodes-base.slack",
      "typeVersion": 2.2,
      "position": [
        1540,
        640
      ],
      "id": "alert-finance-that-it-broke",
      "name": "Alert finance that it broke"
    },
    {
      "parameters": {
        "width": 700,
        "height": 320,
        "content": "## Read the invoice\n\nA Gmail label is the inbox. Anything that is not a PDF is dropped before it costs a model call.\n\nThe extractor is told to return null rather than guess, because a guessed total is worse than a blank one: a blank stops the workflow, a guess gets paid.\n\n**Set up**\n1. Connect Gmail and pick the label your suppliers send to.\n2. Connect an OpenAI credential on the Extraction model node.",
        "color": "6"
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -280,
        -140
      ],
      "id": "sticky-note",
      "name": "Sticky Note"
    },
    {
      "parameters": {
        "width": 880,
        "height": 320,
        "content": "## Check it the way a clerk would\n\nThree sheets are read, then one Code node applies ten checks:\n\n1. The model actually read supplier, number, date and total, and the total is an amount rather than a word\n2. The supplier is on the master AND is still marked active\n3. The tax registration number on the invoice matches the one we hold for that supplier\n4. The invoice number is not already in the ledger\n5. Subtotal plus tax equals the total\n6. The tax rate is one that exists here\n7. A 15 digit tax number is present when tax is charged\n8. The purchase order is open, has value left and is in the same currency\n9. The date is not in the future, and not so old it is probably a second claim\n10. A credit note never posts itself, because only a person knows which bill it belongs against\n\nTolerances, the approval limit and the valid tax rates sit at the top of the Code node.",
        "color": "6"
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        640,
        -140
      ],
      "id": "sticky-note1",
      "name": "Sticky Note1"
    },
    {
      "parameters": {
        "width": 560,
        "height": 320,
        "content": "## Post, hold, or send it back\n\nClean and inside the limit goes straight to the Ledger sheet.\n\nClean but above the limit goes to Slack for a signature. Nothing is posted until someone answers.\n\nAnything that failed a check goes to the Exceptions sheet and the supplier gets a numbered list of what to fix, which is what stops the same broken invoice arriving again.\n\n**The Ledger and Exceptions sheets need these column headers**, in any order,\nbecause rows are mapped by name:\n\n`decision`, `reason`, `failed_checks`, `failures`, `warnings`, `supplier`,\n`supplier_email`, `approver_email`, `invoice_number`, `po_number`,\n`issue_date`, `due_date`, `currency`, `subtotal`, `tax_amount`, `total`,\n`checked_at`\n\nA header that is missing means that column is silently dropped.",
        "color": "6"
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        1700,
        -320
      ],
      "id": "sticky-note2",
      "name": "Sticky Note2"
    },
    {
      "parameters": {
        "width": 520,
        "height": 240,
        "content": "## When it breaks\n\nThe error trigger posts to the same Slack channel with the node that failed and the message. Nothing is half posted, because the ledger write is the last step.",
        "color": "6"
      },
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        1280,
        820
      ],
      "id": "sticky-note3",
      "name": "Sticky Note3"
    }
  ],
  "connections": {
    "Invoice arrives by email": {
      "main": [
        [
          {
            "node": "Keep only PDF attachments",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Keep only PDF attachments": {
      "main": [
        [
          {
            "node": "Read the PDF",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read the PDF": {
      "main": [
        [
          {
            "node": "Pull the invoice fields",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Extraction model": {
      "ai_languageModel": [
        [
          {
            "node": "Pull the invoice fields",
            "type": "ai_languageModel",
            "index": 0
          }
        ]
      ]
    },
    "Pull the invoice fields": {
      "main": [
        [
          {
            "node": "Supplier master",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Supplier master": {
      "main": [
        [
          {
            "node": "Open purchase orders",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Open purchase orders": {
      "main": [
        [
          {
            "node": "Invoices already posted",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Invoices already posted": {
      "main": [
        [
          {
            "node": "Run the checks",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run the checks": {
      "main": [
        [
          {
            "node": "Did every check pass?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Did every check pass?": {
      "main": [
        [
          {
            "node": "Inside the approval limit?",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Park it in the exceptions queue",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Inside the approval limit?": {
      "main": [
        [
          {
            "node": "Post it to the ledger",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Ask a human to sign it off",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Park it in the exceptions queue": {
      "main": [
        [
          {
            "node": "Tell the supplier what is wrong",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "If the run breaks": {
      "main": [
        [
          {
            "node": "Alert finance that it broke",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "pinData": {},
  "settings": {
    "executionOrder": "v1"
  },
  "meta": {
    "instanceId": "finamatik-ops"
  }
}
